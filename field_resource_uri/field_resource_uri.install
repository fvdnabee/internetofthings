<?php

function field_resource_uri_field_schema($field) {

  if ($field['type'] == 'field_resource_uri') {
    $schema['columns']['uri'] = array(
      'type' => 'varchar',
      'length' => '255',
      'not null' => FALSE,
    );

    $schema['indexes'] = array(
      'uri' => array('uri'),
    );

    return $schema;
  }
}